#!/usr/bin/env sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "Running formatting validation (whitespace)..."
dotnet format SpaceExplorationGame.slnx whitespace --verify-no-changes